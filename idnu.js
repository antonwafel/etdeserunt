// Sample array
let cb = [1, 2, 3, 4, 5];

// Using forEach to iterate through the array
cb.forEach((element) => {
  // Perform an operation on each element
  console.log(element * 2); // For example, doubling each element
});
